In file tests/unittests/resources/DiagnosticsSuite/compileTime/unbound_but_namespace.ark:3
    1 | (import package.b)
    2 |
    3 | (print bar)
      |        ^~~
    4 |
        Unbound variable "bar" (did you mean "b:bar"? You either forgot to import it in the symbol list (eg `(import b :bar)') or need to fully qualify it by adding the namespace)
