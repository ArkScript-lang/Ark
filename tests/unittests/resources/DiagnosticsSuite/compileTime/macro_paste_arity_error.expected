At ($as-is b []) @ 1:10
    1 | (macro a ($as-is b []))
      | │        └─ error
      | │
      | └─ macro expansion started here
    2 | (print a)
    3 |
        When expanding `$as-is', expected one argument, got 2 arguments
