{
(import "Reverse.ark")
    # --------------------------
    #           Reverse
    # --------------------------
    (let reverse-tests (fun () {
        (assert (= "CBA" (reverse "ABC")) "Reverse test 1 failed")
        (assert (= "Wow, it works" (reverse (reverse "Wow, it works"))) "Reverse test 2 failed")
    }))
    (reverse-tests)
}