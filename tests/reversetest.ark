{
    (import "Reverse.ark")
    # --------------------------
    #           Reverse
    # --------------------------
    (let reverse-tests (fun () {
        (assert (= "CBA" (reverse "ABC")) "Slice test 1 failed")
        (assert (= "Wow, it work" (reverse (reverse "Wow, it work"))) "Slice test 2 failed")
    }))
    (reverse-tests)
}