(import some.important :code :base)

(let a (if (= 5 code) 1 "4"))

(mut foo (fun (bar egg &a) {
    (let yes egg)
    (+ 1 bar)}))

(if (foo a)
    (print a " is a very interesting variable"))

(while true {
    (foo true nil false)})
