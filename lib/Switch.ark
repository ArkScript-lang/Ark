(let switch (fun (value tests) {
    (let acc 0)
    (let end (len tests))

    (while (!= acc end) {
        (let r (@ tests acc))
        (let bis (@ r 0))
        (if (= value bis) {(@ r 1) (set acc end)} ())

        (set acc (+ 1 acc))
    })
}))