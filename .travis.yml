services: docker

matrix:
  include:
    - os: linux
      dist: xenial
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - gcc-8
            - g++-8
      env:
        - C_COMPILER=gcc-8 CXX_COMPILER=g++-8

    # need MacOS 10.15 to work, travis has only the 10.14
    #- os: osx
    #  osx_image: xcode11.2
    #  compiler:
    #      - clang
    #      - gcc
    #  addons:
    #    homebrew:
    #      packages:
    #        - llvm
    #      update: true
    #  env:
    #    - C_COMPILER=clang CXX_COMPILER=clang++

    - os: linux
      dist: xenial
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
            - llvm-toolchain-xenial-9
            #- sourceline: 'deb http://apt.llvm.org/trusty/ llvm-toolchain-xenial-9 main'
            #  key_url: 'https://apt.llvm.org/llvm-snapshot.gpg.key'
          packages:
            - clang-9
            - libc++-9-dev
            - libstdc++-9-dev
            - libc++abi-9-dev
      env:
        - MATRIX_EVAL="CC=clang-9 && CXX=clang++-9"
        - C_COMPILER=clang-9 CXX_COMPILER=clang++-9
        - LINKER_FLAGS="-stdlib=libc++ -lc++abi"
        - CXXFLAGS="-stdlib=libc++"
        - LIBCXX=On

    - os: windows
      dist: 1803-containers
      env: VERSION=1.11 VARIANT=windows/windowsservercore-1803

sudo: required

language:
  - cpp

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test

script: ./.github/travis.sh
