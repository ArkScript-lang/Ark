!{_> (first ...args) {
    !{if (> (len args) 0)
        ((_> ...args) first)
        first
    }
}}

(let filename "hello.json")
(let io:resource (fun (file) (print file)))
(let io:file (fun (name) {name}))
(let slurp (fun (a) {a}))
(let read-string (fun (a) {a}))

(_> filename io:resource io:file slurp read-string)