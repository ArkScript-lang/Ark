!{bar (a ...args) (print a " " (len args))}
!{egg (...args) (bar ...args)}
(egg 1)

# (egg)  # should fail at compile time

!{get_symbol (bloc) (@ bloc 1)}
!{define (bloc) (let (get_symbol bloc) (@ bloc 2))}
(define (let a 12))
(print a)